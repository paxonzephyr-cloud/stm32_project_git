
---

# STM32 开发学习模板使用说明（基于 EIDE + Keil ARMCC）

## 一、开发环境概览

- **VS Code** + 插件 **Embedded IDE（EIDE）**  
- **Keil MDK**（仅使用 ARMCC 编译器，无需完整 Keil 工程）  
- **OpenOCD**（用于烧录与调试）

> 注：本说明专注于 **将现有 Keil 项目导入 EIDE 并进行开发**，不涉及从零创建工程。

---

## 二、导入 Keil 项目

### 1. 设置工具链（Keil ARMCC）

- 打开 VS Code，确保已安装 EIDE 插件。  
- 点击左侧活动栏的 EIDE 图标，进入 EIDE 界面。  
- 在顶部菜单栏找到 **操作** → **设置工具链**。  
- 选择 **Keil MDK**，然后在弹出的对话框中指定 **Keil 安装目录下的 UV4.exe**（通常位于 `C:\Keil_v5\UV4\UV4.exe`）。  
- 点击确认，EIDE 会自动检测 ARMCC 编译器路径。

### 2. 导入项目

- 在 EIDE 界面中，点击 **操作** → **导入项目**。  
- 选择 **Keil MDK** 类型。  
- 浏览并选择你的 **`.uvprojx`** 或 **`.uvproj`** 文件（Keil 工程文件）。  
- 确认后，EIDE 会解析工程结构并生成对应的 `.eide` 工作区。

### 3. 添加源码文件路径（重要）

由于 Keil 工程中的文件路径可能是相对路径，导入后 EIDE 可能无法直接识别所有源文件。需要手动添加：

- 在 EIDE 的 **项目资源** 区域，右键点击项目名称 → **新建文件夹**（建议使用 **虚拟文件夹**，名称可与原 Keil 分组一致）。  
- 右键新建的文件夹 → **添加文件**，将对应的 `.c` / `.h` 文件加入。  
- 若文件分散在多个目录，还需在 **C/C++ 属性** 中添加 **包含路径**：  
  - 点击项目名称旁的齿轮图标（或右键 → **属性**）。  
  - 在 **C/C++ 构建** → **包含路径** 中，添加所有实际存放头文件的文件夹（**必须使用纯英文路径**）。

> 建议：将项目源文件统一整理到英文路径下，避免中文或空格导致编译错误。

---

## 三、构建与烧录配置

### 1. 设置构建选项

- 在 EIDE 顶部工具栏，点击 **构建配置** 下拉框，选择 **AC5**（即 ARMCC 5 编译器）。  
- 点击 **构建器选项**（小齿轮图标）：  
  - 在 **连接器** 页面中，**输出格式** 选择 **生成 axf**（用于调试），并勾选 **生成 hex**（用于烧录）。  
  - 可根据需要修改优化等级等其他选项。

### 2. 配置烧录器（OpenOCD）

- 在 EIDE 界面左侧，找到 **烧录配置** 区域。  
- 选择 **烧录器类型** 为 **OpenOCD**。  
- 配置以下参数：  
  - **OpenOCD 路径**：指定 `openocd.exe` 的完整路径（例如 `D:/openocd-v0.12.0/bin/openocd.exe`）。  
  - **目标配置文件**：选择对应芯片的配置文件，如 `target/stm32f1x.cfg`。  
  - **接口配置文件**：选择调试器接口，如 `interface/stlink.cfg`（若使用 ST-Link）。  
- 保存配置后，即可点击 EIDE 工具栏的 **烧录** 按钮（🔥图标）进行程序下载。

---

## 四、调试配置

EIDE 配合 **Cortex-Debug** 插件可实现一键调试，无需手动编写 `launch.json`。

### 1. 安装必要插件与工具

- 在 VS Code 扩展商店搜索并安装 **Cortex-Debug** 插件。  
- 在 EIDE 内自动获取调试所需工具：  
  - 点击 EIDE 的 **操作** → **安装使用工具**。  
  - 在列表中搜索并安装 **STM32CubeProgrammer CLI** 和 **STLink GDB Server**（若使用 ST-Link）。  
  - 同时确保系统已安装 **arm-none-eabi-gcc** 工具链（用于 GDB 调试），可在 EIDE 中一并安装或自行下载。

### 2. 启动调试

- 确认当前激活的项目已正确生成 `.axf` 调试文件。  
- 点击 VS Code 右上角的 **调试按钮**（小虫子图标），选择 **Cortex-Debug** 配置。  
- 调试将自动启动，无需额外配置。调试时支持断点、单步、变量查看等功能。

### 3. 查看寄存器（SVD 文件）

为了在调试时查看外设寄存器值，需要提供芯片的 SVD 文件：

- **方法一**：使用 EIDE 自动提取的芯片支持包（SVD 文件内置于 Pack 中）。在 EIDE 的 **芯片支持包** 区域点击对应芯片即可。  
- **方法二**（推荐）：手动获取 SVD 文件并放入工程目录。  
  - 在 Keil 安装目录下的 `ARM\Pack` 中找到对应厂商的 Pack，解压后提取 `.svd` 文件。  
  - 将 `.svd` 文件复制到项目根目录（或任意英文路径）。  
  - 在 EIDE 的 **项目属性** → **调试** 中指定 SVD 文件路径。  
- 采用方法二可避免下载完整 Pack，节省磁盘空间。

---

## 五、进阶功能

### 1. 芯片擦除（Erase）

在烧录测试后，若希望快速清除芯片内程序（例如避免上电后自动运行 LED 闪烁等），可配置一键擦除任务。

在 `.vscode/tasks.json` 中添加如下任务（根据实际 OpenOCD 路径修改）：

```json
{
    "label": "Erase Chip",
    "type": "shell",
    "command": "D:/openocd/bin/openocd.exe",
    "args": [
        "-f", "interface/stlink.cfg",
        "-f", "target/stm32f1x.cfg",
        "-c", "init",
        "-c", "halt",
        "-c", "stm32f1x mass_erase 0",
        "-c", "reset run",
        "-c", "exit"
    ],
    "group": "build",
    "problemMatcher": []
}
```

执行任务：`Ctrl+Shift+P` → 输入 `Tasks: Run Task` → 选择 `Erase Chip` 即可擦除芯片。

### 2. 多目标（Target）管理

EIDE 支持为同一项目创建多个“目标”（Target），每个目标可独立控制哪些文件参与编译，非常适合创建 **example** 目录下不同 main 函数的示例。

- 在 EIDE 项目资源中，右键项目名称 → **切换目标** → **新建目标**。  
- 新目标会复制当前目标的配置。  
- 在 **项目资源** 中，右键任意文件 → **从项目中移除**，该文件将只从当前目标中排除，不影响其他目标。  
- 利用此功能，可以在一个项目中同时保留多个 `main.c` 入口，通过切换目标选择编译哪个示例。

---

## 六、常见问题与注意事项

| 问题 | 解决方法 |
|------|----------|
| 编译时提示找不到头文件 | 检查 **C/C++ 包含路径** 是否添加了所有头文件所在目录，且路径为纯英文。 |
| 烧录时报错 “openocd: command not found” | 检查烧录配置中的 OpenOCD 路径是否正确，且路径中不包含空格。 |
| 烧录时报错 "没有找到hex" | 点击左侧 EIDE 面板,找到 Builder Options（构建选项）,在 C/C++ Compiler中将不生成二进制文件取消勾选 |
| 调试无法启动，提示 GDB 未找到 | 确认已安装 `arm-none-eabi-gdb`，并在 EIDE 的 **安装使用工具** 中正确配置路径。 |
| 导入后工程文件缺失 | 由于 Keil 工程使用相对路径，可能需要在 EIDE 中手动添加源文件并设置包含路径。建议将工程整体迁移到纯英文目录后再导入。 |
| SVD 文件未加载，寄存器值显示为 0 | 检查 SVD 文件路径是否正确，并在调试时手动选择加载（通过 Cortex-Debug 插件配置）。 |
| printf重定向后程序卡死 | 点击左侧 EIDE 面板,找到 Builder Options（构建选项）,在 C/C++ Compiler 或 Linker 里找到 microlib 并启用 |
---

## 七、参考资料

- [Embedded IDE 官方文档](https://em-ide.com/docs/intro)  
- [OpenOCD 配置文件说明](https://openocd.org/doc/html/General-Commands.html)  
- [Cortex-Debug 插件文档](https://github.com/Marus/cortex-debug)

---

### 主要改进说明

1. **结构重组**：按照“导入 → 构建 → 烧录 → 调试 → 进阶”的逻辑流程，步骤更清晰。
2. **补充关键细节**：强调了**纯英文路径**的重要性、手动添加文件与包含路径的必要性、SVD 文件的两种获取方式。
3. **增加常见问题表格**：解决初学者最易遇到的坑。
4. **统一术语**：使用“EIDE”代替“vs code embedded”，明确插件名称。
5. **保留原有特色内容**：擦除任务、多目标切换等实用功能得以保留并加以润色。

如需进一步补充（如针对特定芯片型号的配置差异），可在此基础上扩展。